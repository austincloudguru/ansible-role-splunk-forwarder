---
dependency:
  name: galaxy
driver:
  name: docker
lint: |
  set -e
  yamllint .
  ansible-lint
platforms:
  - name: instance
    image: ${MOLECULE_DISTRO}
    privileged: true
    command: /sbin/init
provisioner:
  name: ansible
verifier:
  name: testinfra
  directory: ./tests
